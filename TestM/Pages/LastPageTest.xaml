<Page x:Class="TestM.Pages.LastPageTest"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TestM.Pages"
      xmlns:vm="clr-namespace:TestM.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="585"
      d:DesignWidth="1190"
      Title="LastPageTest">
    <Page.DataContext>
        <vm:LastPageTestViewModel/>
    </Page.DataContext>
    <Grid Background="Transparent">
        <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel Name="AllResult"
                        Orientation="Vertical">
                <TextBlock Grid.Row="1"
                           Name="Result"
                           Style="{DynamicResource TextBlockPoints}"/>
                <TextBlock Style="{DynamicResource TextBlockResult}"
                           Text="Вопросы в которых Вы допустили ошибки:"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
